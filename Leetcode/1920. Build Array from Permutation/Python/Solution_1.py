class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        solution = []

        for n in nums:
            solution.append(nums[n])
        
        return solution

#no follow up ( brute force )